import React from "react";
import { Alert, Pressable, ScrollView, Text, View } from "react-native";
import { useAuth } from "../AuthContext";
import { colors, ui } from "../theme";
export function ProfileScreen() { const { user, signOut } = useAuth(); return <ScrollView style={ui.screen} contentContainerStyle={ui.scroll}><Text style={ui.eyebrow}>ACCOUNT</Text><Text style={ui.title}>Your profile.</Text><Text style={ui.subtitle}>Manage your Femantic mobile session.</Text><View style={[ui.panel, { marginTop: 25 }]}><Text style={{ color: colors.text, fontSize: 20, fontWeight: "900" }}>{user?.full_name || "Femantic user"}</Text><Text style={ui.subtitle}>{user?.email}</Text><View style={{ borderTopColor: colors.line, borderTopWidth: 1, marginTop: 17, paddingTop: 15, flexDirection: "row", justifyContent: "space-between" }}><Text style={{ color: colors.muted }}>Plan</Text><Text style={{ color: colors.green, fontWeight: "900", textTransform: "uppercase" }}>{user?.membership}</Text></View><View style={{ flexDirection: "row", justifyContent: "space-between", marginTop: 13 }}><Text style={{ color: colors.muted }}>Role</Text><Text style={{ color: colors.text, fontWeight: "800", textTransform: "uppercase" }}>{user?.role}</Text></View></View><Pressable style={[ui.outlineButton, { marginTop: 20 }]} onPress={() => Alert.alert("Sign out?", "You will need to sign in again on this device.", [{ text: "Cancel", style: "cancel" }, { text: "Sign out", style: "destructive", onPress: signOut }])}><Text style={ui.outlineText}>SIGN OUT</Text></Pressable><Text style={{ color: colors.muted, fontSize: 12, marginTop: 25, textAlign: "center" }}>Femantic Analytics Mobile 1.0.0</Text></ScrollView>; }
